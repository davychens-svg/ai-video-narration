# Server Configuration
HOST=0.0.0.0
PORT=8001

# Model Configuration
DEFAULT_MODEL=qwen2vl
# Options: smolvlm, qwen2vl, moondream, mobilevlm
MODEL_DEVICE=cuda
# Options: cuda, cpu, mps
MODEL_DTYPE=float16
# Options: float16, float32, bfloat16

# CORS Configuration
# Comma-separated list of allowed origins
# Use "*" to allow all origins (not recommended for production)
# Example for production: http://your-domain.com,https://your-domain.com
ALLOWED_ORIGINS=http://172.233.92.213,https://172.233.92.213,http://localhost:3000

# Performance Configuration
MAX_WORKERS=4
# Number of worker processes for processing frames

# Logging
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR
